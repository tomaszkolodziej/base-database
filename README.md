# base-database

Uruchomienie:

Zbudowanie aplikacji: mvn clean install

Zbudowanie aplikacji i obrazu: mvn clean package

Przejść do katalogu główne i wykonać polecenie: docker-compose up --build

Aplikacja uruchomi się i wykona skrypty aktualizujące bazę danych.

